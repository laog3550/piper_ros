# 项目约定

## 语言

本项目的所有文档一律用中文书写，这是一条底层约定：

- `docs/` 下的所有 Markdown 文档
- 面向人的接口说明，例如 `.msg` / `.srv` 文件中的注释
- 新增的说明性文本、报告、操作手册

命令、代码标识符、话题名、服务名、CAN ID、度量单位等技术记号保持原样，
不翻译。仓库既有文件中残留的英文文档应逐步改写为中文。

代码内部的 docstring 与行内注释不受本条约束，按所在文件的既有风格书写。
